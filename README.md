# 🐍 GameSnake

Браузерная игра "Змейка" с автоматическим CI/CD pipeline через Jenkins.

## 🎮 Игра

Классическая игра "Змейка" на HTML5 Canvas. Управление стрелками на клавиатуре.

## 🚀 Быстрый старт

### Локальный запуск

Просто откройте `index.html` в браузере.

### Запуск через Docker

```bash
docker build -t snake-game .
docker run -d -p 80:80 --name snake-game snake-game
```

Приложение будет доступно по адресу: http://localhost

### Запуск через Docker Compose

```bash
docker-compose up -d
```

## 🔧 CI/CD

Проект настроен для автоматического деплоя через Jenkins.

### Структура CI/CD

- **Jenkinsfile** - основной pipeline для сборки и деплоя
- **Jenkinsfile.compose** - альтернативный вариант с использованием docker-compose
- **docker-compose.yml** - конфигурация для деплоя через compose

### Настройка

Подробные инструкции по настройке CI/CD находятся в файле [JENKINS_SETUP.md](JENKINS_SETUP.md).

### Pipeline этапы

1. **Checkout** - получение кода из репозитория
2. **Build Docker Image** - сборка Docker образа
3. **Stop Old Container** - остановка старого контейнера
4. **Deploy** - запуск нового контейнера
5. **Cleanup** - очистка старых образов

## 📁 Структура проекта

```
.
├── index.html          # Главная страница игры
├── script.js           # Логика игры
├── style.css           # Стили
├── Dockerfile          # Docker образ на базе nginx
├── docker-compose.yml  # Конфигурация для compose
├── Jenkinsfile         # CI/CD pipeline
├── .dockerignore       # Исключения для Docker
└── README.md           # Документация
```

## 🌐 Деплой

После настройки CI/CD, при каждом push в репозиторий будет автоматически:
- Собираться новый Docker образ
- Останавливаться старый контейнер
- Запускаться новый контейнер с обновленным кодом

Приложение доступно по адресу: **http://192.168.1.102**

## 📝 Лицензия

MIT